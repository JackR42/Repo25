# SQL Server SA password – copy this file to .env and set a strong password
MSSQL_SA_PASSWORD=YourStrong@Passw0rd

# Optional host port for SQL Server container -> container 1433
# Change this if the default host port is already in use.
SQL_HOST_PORT=14330
